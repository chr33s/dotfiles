#!/usr/bin/env zsh

setopt append_history share_history histignorealldups
bindkey -e

. ${HOME}/.zsh/exports
. ${HOME}/.zsh/aliases
. ${HOME}/.zsh/functions
. ${HOME}/.zsh/plugins
. ${HOME}/.zsh/theme

clear
