#!/usr/bin/env bash

server() {
  local port="${1:-8000}"

  if ! command -v python3 >/dev/null 2>&1; then
    echo "server: python3 is not installed" >&2
    return 1
  fi

  sleep 1 && open "http://localhost:${port}/" &
  python3 -m http.server "$port"
}

server "$@"
