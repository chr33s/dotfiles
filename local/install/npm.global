#!/usr/bin/env bash

if test ! $(which npm); then

  packages=(
    prettier
    eslint
    eslint-config-prettier
    eslint-plugin-prettier
    eslint-plugin-react
    babel-eslint
    svgo
    n
    react-native-cli
    react-native-git-upgrade
  )

  npm -g ${packages[@]}

fi
